apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "smocker.fullname" . }}-test-connection"
  labels:
    {{- include "smocker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "smocker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
